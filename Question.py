class Question:
    # Define a class named Question with a constructor that takes 'prompt' and 'answer' as parameters.
    def __init__(self, prompt, answer):
         # Initialize instance variables 'prompt' and 'answer' with the values provided as arguments.
        self.prompt = prompt
        self.prompt = prompt
        self.answer = answer

